# Local Vite proxies /api → localhost:4000, so this can stay empty.
# Production (Vercel): set this in the Vercel project env vars (Production).
# No trailing slash.
# VITE_API_URL=https://ultrix-leave-api.onrender.com
VITE_API_URL=
